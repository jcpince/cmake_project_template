set(PROJECT_INCLUDES
  ${PROJECT_INCLUDES}
  ${CMAKE_CURRENT_SOURCE_DIR}/include
  PARENT_SCOPE)

if(NOT USE_FEATURE_TEST3)
  return()
endif()

aux_source_directory(${CMAKE_CURRENT_SOURCE_DIR} SUB_SOURCES)

#set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -DUSE_FEATURE_TEST3" PARENT_SCOPE)

set(SOURCE
  ${SOURCE}
  ${SUB_SOURCES}
  PARENT_SCOPE)
